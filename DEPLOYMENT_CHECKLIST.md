# Чеклист готовности к деплою

## ✅ Что уже готово

1. **Код приложения**
    - ✅ FastAPI приложение настроено корректно
    - ✅ Все пути относительные (нет hardcoded путей)
    - ✅ База данных SQLite использует относительный путь
    - ✅ Статические файлы настроены правильно
    - ✅ Обработка ошибок реализована

2. **Зависимости**
    - ✅ `requirements.txt` содержит все необходимые пакеты
    - ✅ `package.json` настроен для сборки Tailwind CSS
    - ✅ Все зависимости указаны

3. **Конфигурация**
    - ✅ Telegram бот использует переменные окружения
    - ✅ Нет hardcoded токенов или секретов в коде
    - ✅ `.gitignore` исключает чувствительные файлы

## ⚠️ Что нужно сделать перед деплоем

### Вариант 1: Автоматический деплой (рекомендуется)

1. **Отредактировать `deploy.sh`:**
    - Указать `PROJECT_REPO` (URL вашего репозитория)
    - Указать `DOMAIN` (ваш домен)
    - Указать `BOT_TOKEN` (токен Telegram бота)
    - Указать `ADMIN_IDS` (ваши Telegram ID)
    - Указать `SSL_EMAIL` (email для Let's Encrypt)

2. **Запустить скрипт:**
    ```bash
    sudo bash deploy.sh
    ```

Скрипт выполнит все необходимые шаги автоматически.

### Вариант 2: Ручной деплой

### 1. Создать файл `.env` на сервере

```bash
# На сервере создайте .env файл:
BOT_TOKEN=your_actual_bot_token
ADMIN_IDS=your_telegram_user_id
```

**Важно:** Файл `.env` не должен попадать в git (уже в `.gitignore`)

### 2. Собрать CSS перед деплоем

```bash
npm run build
```

Это создаст минифицированный `static/css/output.css`

### 3. Настроить права доступа

```bash
# Убедитесь, что у процесса есть права на запись в директорию проекта
# (для создания/обновления базы данных)
chmod -R 755 /path/to/Remont
```

### 4. Выбрать способ запуска

- **Systemd** (рекомендуется) - автоматический запуск при перезагрузке
- **Screen/Tmux** - простой вариант для тестирования
- **Скрипты** - `start.sh` и `start-bot.sh`

### 5. Настроить firewall (если нужно)

```bash
# Разрешить порт 8000 (если не используете Nginx)
sudo ufw allow 8000/tcp
```

### 6. Настроить Nginx (опционально, но рекомендуется)

- Reverse proxy для FastAPI
- SSL сертификат (Let's Encrypt)
- См. инструкции в `DEPLOYMENT.md`

## 🔍 Проверка после деплоя

1. **Веб-приложение доступно:**

    ```bash
    curl http://localhost:8000
    ```

2. **Telegram бот отвечает:**
    - Отправьте `/applications` боту
    - Должен ответить (если вы администратор)

3. **Формы работают:**
    - Откройте сайт в браузере
    - Отправьте тестовую заявку
    - Проверьте, что она появилась в базе данных

4. **Логи без ошибок:**
    ```bash
    sudo journalctl -u remont-web.service -n 50
    sudo journalctl -u remont-bot.service -n 50
    ```

## 📝 Рекомендации для production

1. **Мониторинг:**
    - Настройте мониторинг логов
    - Настройте алерты при падении сервисов

2. **Резервное копирование:**
    - Настройте автоматический бэкап `applications.db`
    - Храните бэкапы в отдельном месте

3. **Безопасность:**
    - Используйте HTTPS (через Nginx + Let's Encrypt)
    - Ограничьте доступ к порту 8000 только через Nginx
    - Регулярно обновляйте зависимости

4. **Производительность:**
    - Настройте количество workers в uvicorn (уже 4 в скриптах)
    - Рассмотрите использование PostgreSQL вместо SQLite для больших нагрузок

## 🚨 Известные ограничения

1. **SQLite база данных:**
    - Подходит для небольших/средних нагрузок
    - Для высоких нагрузок рассмотрите PostgreSQL

2. **Один экземпляр:**
    - Приложение не настроено для горизонтального масштабирования
    - Для масштабирования нужна внешняя БД и shared storage

## ✅ Итоговая проверка

- [ ] `.env` файл создан и заполнен
- [ ] CSS собран (`npm run build`)
- [ ] Зависимости установлены (`pip install -r requirements.txt`, `npm install`)
- [ ] Сервисы запущены (systemd или вручную)
- [ ] Веб-приложение доступно
- [ ] Telegram бот работает
- [ ] Формы отправки заявок работают
- [ ] Логи не содержат ошибок
- [ ] Nginx настроен (если используется)
- [ ] SSL сертификат установлен (если используется)
- [ ] Резервное копирование настроено

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `sudo journalctl -u remont-web.service -f`
2. Убедитесь, что все переменные окружения установлены
3. Проверьте права доступа к файлам
4. См. раздел "Устранение неполадок" в `DEPLOYMENT.md`
